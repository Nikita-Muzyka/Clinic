<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:hc="http://handyorg.com/winfx/xaml/controls"
                    xmlns:controls="https://handyorg.github.io/handycontrol">

    <Style TargetType="RadioButton" x:Key="MainRadioButton">
        <Setter Property="Background" Value="#1e2e3b"></Setter>
        <Setter Property="Foreground" Value="White"></Setter>
        <Setter Property="FontSize" Value="14"></Setter>
        <Setter Property="FontFamily" Value="/Resources/Font/Sansation/Sansation-Regular.ttf#Sansation"></Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <Border Background="{TemplateBinding Background}"
                            CornerRadius="0">
                        <Grid>
                            <Ellipse Fill="{TemplateBinding Background}"
                                     Height="10"
                                     Width="10"
                                     HorizontalAlignment="Left"
                                     Margin="10"
                                     Name="elRadio"></Ellipse>
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center">

                            </ContentPresenter>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter Property="Background" Value="#46a7b2"/>
                            <Setter Property="Foreground" Value="White"></Setter>
                            <Setter TargetName="elRadio" Property="Fill" Value="#46a7b2"></Setter>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>