<Window x:Class="Clinic.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        xmlns:local="clr-namespace:Clinic"
        mc:Ignorable="d"
        Title="MainWindow" Height="800" Width="1200">
    <Window.Resources>
        <ResourceDictionary Source="/Theme/RadioButtonMainTheme.xaml"/>
    </Window.Resources>

    <Grid ShowGridLines="True">
        <Grid.RowDefinitions>
            <RowDefinition Height="40"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <TextBlock HorizontalAlignment="Center"
                   FontSize="18"
                   VerticalAlignment="Center">HOSPITAL "-"
        </TextBlock>

        <RadioButton Grid.Row="1" 
                     Margin="0,10,0,0"
                     Click="Main_RadioButton"
                     Style="{DynamicResource MainRadioButton}" 
                     VerticalAlignment="Top" 
                     Width="140">Главная</RadioButton>
        <RadioButton Grid.Row="1" 
                     Margin="0,28,0,50"
                     Click="Reception_RadioButton"
                     Style="{DynamicResource MainRadioButton}" >Привем</RadioButton>
        <RadioButton Grid.Row="1" 
                     Margin="0,55,0,22"
                     Click="RegistrationPage_Button"
                     Style="{DynamicResource MainRadioButton}" >Регистрация</RadioButton>
        <RadioButton Grid.Row="1" 
                     Margin="0,78,0,2"
                     Click="ClinicPersonal_Button"
                     Style="{DynamicResource MainRadioButton}" >Персонал</RadioButton>

        <Frame Grid.Row="1"
               Grid.Column="1"
               NavigationUIVisibility="Hidden"
               Name="MainFrame"></Frame>
    </Grid>
</Window>
